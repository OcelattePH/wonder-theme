{% doc %}
  Renders a quantity input with minus/plus buttons.
  Relies on the QuantityInput custom element defined in global.js.

  @param {string} id - The input ID for label association
{% enddoc %}

<quantity-input class="quantity">
  <button class="quantity__button" name="minus" type="button" aria-label="{{ 'products.product.quantity.decrease' | t }}">
    {% render 'icon-minus' %}
  </button>
  <input
    class="quantity__input"
    type="number"
    name="quantity"
    id="{{ id }}"
    min="1"
    value="1"
  >
  <button class="quantity__button" name="plus" type="button" aria-label="{{ 'products.product.quantity.increase' | t }}">
    {% render 'icon-plus' %}
  </button>
</quantity-input>
